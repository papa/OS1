//
// Created by os on 5/21/22.
//

#include "../h/threadWrapperFunctions.hpp"
#include "../h/syscall_cpp.hpp"

void periodicRunner(void* t)
{
    /*PeriodicThread* pthr = (Thread*)t;
    while(true)
    {
        pthr->periodicActivation();
        Thread::sleep()
    }*/
}

void regularRunner(void* t)
{
    //Thread* thr = (Thread*)t;
    //thr->run();
}
